import 'package:flutter/material.dart';

class AppConstants {
  static const mainColor = Colors.teal;
  static const whiteColor = Colors.white;
  static const splashScreenColor = Color.fromARGB(169, 0, 150, 135);
}
